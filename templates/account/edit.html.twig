{% extends 'base.html.twig' %}
{% block title %} {{app.user.firstname }} Modifie Ton Compte {% endblock %}

{% block content %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<hr>

</div>
<div class="example-wrapper">
    <h1>Modifie Ton Compte</h1>

  <div class="container container-fluid">
    <div class="row">
      <div class="col-lg-8">
      {{form(form) }}

       {# Vérifier si la route est "event_edit" on afficge les images #}
       {# {% if app.request.attributes.get('_route') == 'event_edit' %}
       <h2>Images</h2> #}
       
       {# {% for image in event.images %}
        <div>
          <img src"={{asset('/img/'~image.name)}}" alt="Image" width="150" >
          <a href="{{path('event_delete_image',{id: image.id}) }}" data-delete data-token="{{csrf_token('delete'~ image.id)}}"> Supprimer</a>        
       {% endfor %}
       {% endif %} #}
       
      </div>
      </div>
      </div>
      <a href="{{ path('profilSuppression',{'id': app.user.id}) }}">>Supprimer votre compte</a><br>


{% endblock %}